# Kanban Board for Unity Editor

Простая канбан-доска прямо в Unity Editor для управления задачами проекта.

## Установка

### Вариант 1: Unity Package
1. В Unity откройте папку `Assets/Editor/Kanban`
2. Правый клик → **Export Package...**
3. Убедитесь что выбраны все файлы (кроме `kanban_board.json` если хотите чистый пакет)
4. Сохраните `.unitypackage`

Для импорта: **Assets → Import Package → Custom Package**

### Вариант 2: Копирование
Скопируйте папку `Assets/Editor/Kanban` в свой проект.

## Использование

Откройте: **Tools → Kanban Board** (или `Ctrl+Shift+K` / `Cmd+Shift+K`)

### Колонки
- 📋 **Backlog** — идеи, на потом
- 📝 **Todo** — запланировано  
- 🔨 **Doing** — в работе
- ✅ **Done** — готово

### Приоритеты
- 🔴 Critical (красный)
- 🟠 High (оранжевый)
- 🔵 Normal (синий)
- ⚪ Low (серый)

### Функции
- **Drag & Drop** — перетаскивание карточек между колонками
- **Подзадачи** — чеклист внутри задачи
- **История** — лог всех изменений задачи
- **Архив** — скрытие завершённых задач
- **Поиск** — фильтрация по названию, заметкам, тегам
- **Теги** — категоризация задач (через запятую)
- **Ссылки** — URL на issue/PR

### Горячие клавиши
| Клавиша | Действие |
|---------|----------|
| `Enter` | Добавить задачу (в поле ввода) |
| `Ctrl + ← →` | Переместить выбранную задачу |
| `Ctrl + D` | Дублировать задачу |
| `Ctrl + Delete` | Удалить задачу |
| `Ctrl + Shift + A` | Архивировать все Done |
| `Escape` | Снять выделение |
| `Double Click` | Открыть редактор задачи |

## Хранение данных

Данные хранятся в `Assets/Editor/Kanban/kanban_board.json` — человекочитаемый JSON.

### Работа в команде
- Файл `.json` можно коммитить в Git
- При конфликтах — мержится как обычный текст
- Каждая задача имеет уникальный ID

### Рекомендации для команды
- Добавьте `kanban_board.json` в `.gitignore` если каждый хочет свою доску
- Или коммитьте его если нужна общая доска (мержите конфликты вручную)

## Файлы

```
Assets/Editor/Kanban/
├── KanbanWindow.cs      # Основное окно
├── KanbanData.cs        # Модели данных и хранение
├── kanban_board.json    # Данные доски (создаётся автоматически)
└── README.md            # Этот файл
```

## Требования

- Unity 2021.3+ (использует C# 9 features)
- Работает в любом проекте (Built-in, URP, HDRP)

## Лицензия

MIT — используйте как хотите.

